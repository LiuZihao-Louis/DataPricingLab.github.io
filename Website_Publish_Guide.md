# GitHub Pages 发布说明

1. 新建 GitHub 仓库。
2. 将本目录中的 `index.html`、`styles.css`、`app.js`、`assets/` 上传到仓库根目录。
3. 进入仓库 **Settings → Pages**。
4. 在 **Build and deployment** 中选择 **Deploy from a branch**。
5. 选择 `main` 分支和 `/ (root)`，保存。
6. 等待 GitHub Pages 生成访问链接。

发布后请检查：网页图片是否加载、估值计算器是否可用、移动端表格是否可横向滚动。
